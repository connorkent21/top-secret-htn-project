import { createMuiTheme } from '@material-ui/core/styles';

export const MaterialTheme = createMuiTheme({
  palette: {
    primary: { main: '#B23A48' },
    secondary: { main: '#461220' },
  },
  typography: {
    fontFamily: ['Work Sans'],
  },
});
